Clustering.js cotains the main clustering operations. priorityqueue contains an implementation of priority queues in javascript for use in a part of the clustering. Calling mainClustering(tweets: Array of tweets, sentiments: JSON Object of totaly sentiment of all the text) will return and array of Cluster objects, with each object having a parameter tweets which has the array of tweets in it's cluster.
